app-with-shell:
  image: ubuntu
  depends_on:
    - shell-form

shell-form:
  build:
    dockerfile: Dockerfile.shell-form

app-with-exec:
  image: ubuntu
  depends_on:
    - exec-form

exec-form:
  build:
    dockerfile: Dockerfile.exec-form
